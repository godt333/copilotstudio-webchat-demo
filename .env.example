# =============================================================================
# M365 Agents SDK Configuration for Copilot Studio Integration
# =============================================================================

# Azure AD App Registration (REQUIRED)
# Create a Single-Page Application (SPA) in Azure AD with:
# 1. Redirect URI: http://localhost:5174 (development) and your production URL
# 2. API Permission: CopilotStudio.Copilots.Invoke (Delegated)
VITE_AZURE_AD_CLIENT_ID=your-app-client-id-here
VITE_AZURE_AD_TENANT_ID=your-tenant-id-here

# Copilot Studio Connection (choose one option)
# Option 1: Direct Connect URL from Copilot Studio Channels > Web app
VITE_COPILOT_DIRECT_CONNECT_URL=https://YOUR_ENVIRONMENT.api.powerplatform.com/copilotstudio/dataverse-backed/authenticated/bots/YOUR_BOT_ID/conversations?api-version=2022-03-01-preview

# Option 2: Environment ID and Agent Identifier (alternative to Direct Connect URL)
# Get these from Copilot Studio > Settings > Advanced > Metadata
# VITE_COPILOT_ENVIRONMENT_ID=your-environment-id
# VITE_COPILOT_AGENT_IDENTIFIER=your-schema-name
