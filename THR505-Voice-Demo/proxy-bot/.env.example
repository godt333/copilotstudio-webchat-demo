# ============================================
# DLS Proxy Bot Environment Configuration
# ============================================
# Copy this file to .env and fill in the values

# Azure Bot Service Registration
# Get these from Azure Portal > Bot Services > Your Bot > Configuration
MICROSOFT_APP_ID=your-bot-app-id-here
MICROSOFT_APP_PASSWORD=your-bot-app-password-here
MICROSOFT_APP_TYPE=MultiTenant

# Server Configuration
PORT=3978

# Copilot Studio Connection
# Option 1: Use Token Endpoint (recommended for "No authentication" bots)
COPILOT_TOKEN_ENDPOINT=https://your-environment.api.powerplatform.com/powervirtualagents/botsbyschema/your-bot/directline/token?api-version=2022-03-01-preview

# Option 2: Use Direct Line Secret (if Token Endpoint not available)
# COPILOT_DIRECT_LINE_SECRET=your-direct-line-secret

# Azure Speech Services (for DLS channel in Azure Portal)
# These are used when configuring the DLS channel, not in this code
SPEECH_REGION=eastus
SPEECH_RESOURCE_ID=/subscriptions/YOUR_SUB/resourceGroups/YOUR_RG/providers/Microsoft.CognitiveServices/accounts/YOUR_SPEECH_RESOURCE

# Logging
LOG_LEVEL=info
